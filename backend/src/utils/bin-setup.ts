/**
 * bin-setup.ts — DEPRECATED
 *
 * Binary management is now handled automatically by:
 *   - youtube-dl-exec (bundles yt-dlp binary, downloads at npm install time)
 *   - ffmpeg-static (bundles ffmpeg binary as an npm package)
 *
 * No manual setup required. This file is intentionally empty.
 */
export {};
